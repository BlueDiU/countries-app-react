const initialState = {
  countries: [],
  details: [],
  region: [],
  country: [],
};

export const countryReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
